<UserControl x:Class="HomeEconomicSystem.PL.View.UserControls.GraphUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:chart="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             d:DataContext="{d:DesignInstance local:GraphUC}"
             xmlns:local="clr-namespace:HomeEconomicSystem.PL.View.UserControls"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"   
             TextOptions.TextFormattingMode="Ideal" 
             TextOptions.TextRenderingMode="Auto"       
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid DataContext="{Binding ., RelativeSource={RelativeSource AncestorLevel=1, AncestorType=local:GraphUC}}" FlowDirection="LeftToRight">
        <Grid.Resources>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" DataContext="{Binding Graph}"
                    Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                    HorizontalAlignment="Center"
                   FontWeight="ExtraBold"
                    Text="{Binding Title, TargetNullValue=Title}" />
        <TextBlock  Grid.Row="1"  DataContext="{Binding Graph}"
                    Text="{Binding Description}" 
                    Style="{StaticResource MaterialDesignSubtitle2TextBlock}"
                    HorizontalAlignment="Center"/>
        <chart:CartesianChart 
            Series="{Binding SeriesCollection}" 
            LegendLocation="Right"
            Grid.Row="2">
            <chart:CartesianChart.AxisY>
                <chart:Axis Title="Sales" LabelFormatter="{Binding YFormatter}"></chart:Axis>
            </chart:CartesianChart.AxisY>
            <chart:CartesianChart.AxisX>
                <chart:Axis Title="Month" Labels="{Binding Labels}"></chart:Axis>
            </chart:CartesianChart.AxisX>
        </chart:CartesianChart>
    </Grid>
</UserControl>
